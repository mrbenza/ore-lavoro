/**
 * @file Tests.gs
 * @description Indice dei file di test — le funzioni sono state spostate in file separati.
 *
 * I test manuali sono ora organizzati per modulo in file dedicati:
 *
 *   test_authentication.gs     — testRobustAuthentication(), diagnoseSheetStructure()
 *   test_userapi.gs            — testUserAPI()
 *   test_utils.gs              — testUtils()
 *   test_adminapi.gs           — testDeleteWorkEntry()
 *   test_archivioore.gs        — testSingleArchive()
 *   test_reportcommercialista.gs — testReportSystem(), testSingleReport()
 *   test_config.gs             — testConfig()
 *   test_sheetsDAO.gs          — testSheetsDAO()
 *   test_apirouter.gs          — testConfiguration(), testAuthentication(),
 *                                testGetMonthlyWorkData(), testCompleteSystem()
 */
